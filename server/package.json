{
  "name": "rigi-backend",
  "version": "1.0.0",
  "description": "'backend for rigi video player'",
  "main": "index.js",
  "scripts": {
    "start": "json-server --watch data.json",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "sscodes",
  "license": "MIT",
  "dependencies": {
    "json-server": "^1.0.0-alpha.23"
  }
}
